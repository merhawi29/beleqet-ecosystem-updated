# =============================================================================
# Beleqet Backend — Environment Variables
# Copy this file to .env and fill in your values before running.
# NEVER commit .env to version control.
# =============================================================================

NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000

DATABASE_URL=postgresql://beleqet_user:your_password@localhost:5432/beleqet_db

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS=false

# Generate with: openssl rand -hex 64
JWT_ACCESS_SECRET=REPLACE_WITH_STRONG_SECRET_64_CHARS
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=30d

# Two-Factor Authentication (TOTP)
# Generate with: openssl rand -hex 32 (produces 64 hex chars / 32 bytes for AES-256)
TOTP_ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHARS
TOTP_ISSUER=Beleqet
# Separate signing key for 2FA temp/challenge tokens (REQUIRED, must differ from JWT_ACCESS_SECRET)
# Generate with: openssl rand -hex 64
TOTP_TEMP_SECRET=REPLACE_WITH_STRONG_SECRET_64_CHARS

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Local LLM used for video-interview evaluation (falls back to OpenAI if unreachable)
OLLAMA_URL=http://localhost:11434
# Max bytes streamed for a single interview video download (default 104857600 = 100 MiB)
VIDEO_INTERVIEW_MAX_BYTES=104857600
# Overall download deadline in ms — protects against tarpit / slow-loris streams (default 120000)
VIDEO_INTERVIEW_DOWNLOAD_TIMEOUT_MS=120000
# Optional comma-separated extra hosts allowed for interview videoUrl (SSRF allowlist)
# VIDEO_URL_ALLOWED_HOSTS=cdn.example.com,uploads.example.com

# Resume Brain AI extractor (OpenAI-compatible provider).
# Works with Groq (keys start with gsk_, default endpoint below) OR
# xAI Grok (keys start with xai-, set GROQ_BASE_URL=https://api.x.ai/v1).
# Put the REAL key in .env — never here (this file is committed to git).
GROQ_API_KEY=REPLACE_WITH_YOUR_KEY
GROQ_MODEL=llama-3.1-8b-instant
# Optional endpoint override; defaults to Groq's OpenAI-compatible API.
# GROQ_BASE_URL=https://api.groq.com/openai/v1
# Optional: per-request timeout (ms) and SDK retry count for the extractor.
# GROQ_TIMEOUT_MS=20000
# GROQ_MAX_RETRIES=2
# Per-user daily AI cost budget for /resume-brain/extract (Redis-backed).
# Requests OR tokens over the limit are rejected with 429 for the rest of the
# window. Defaults: 50 extractions, 100000 tokens, 86400s (24h) window.
# RESUME_AI_DAILY_REQUEST_BUDGET=50
# RESUME_AI_DAILY_TOKEN_BUDGET=100000
# RESUME_AI_BUDGET_WINDOW_SECONDS=86400
EXA_API_KEY=EXA_APIKEY
SEARCH_PROVIDER=exa
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHANNEL_ID=-1001234567890

CHAPA_SECRET_KEY=CHASECK_TEST-...
CHAPA_PUBLIC_KEY=CHAPUBK_TEST-...
CHAPA_WEBHOOK_SECRET=your_webhook_secret
CHAPA_CALLBACK_URL=http://localhost:4000/api/v1/escrow/callback
CHAPA_RETURN_URL=http://localhost:3000/freelance/payment-success

STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_MODE=sandbox
# From the PayPal developer dashboard's webhook config — required to verify
# incoming PayPal webhook signatures (unsafe to skip in production).
PAYPAL_WEBHOOK_ID=
PAYPAL_RETURN_URL=http://localhost:3000/payment/success
PAYPAL_CANCEL_URL=http://localhost:3000/payment/cancel

# Subscription Manager: HMAC-SHA256 secret for POST /billing/webhook
# (gateways integrated directly against the Subscription domain — PayPal
# itself is verified via PAYPAL_WEBHOOK_ID above instead).
# Generate with: openssl rand -hex 32
BILLING_WEBHOOK_SECRET=

AWS_S3_BUCKET=beleqet-uploads
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1

# Cloudflare R2 / S3-compatible object storage
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_BASE_URL=

# CDN delivery URL and immutable cache policy
CDN_BASE_URL=
CDN_CACHE_CONTROL=public, max-age=31536000, immutable

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@beleqet.com
SMTP_PASS=your_smtp_password
EMAIL_FROM=Beleqet <noreply@beleqet.com>
SMTP_PASSWORD=
SMTP_SECURE=false
SMTP_FROM=Beleqet <noreply@beleqet.com>

# Initial admin account used by prisma/seed.ts
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_FIRST_NAME=Platform
ADMIN_LAST_NAME=Admin

BULL_BOARD_USERNAME=admin
BULL_BOARD_PASSWORD=your_board_password

# GDPR Compliance — generate with: openssl rand -hex 32
GDPR_ENCRYPTION_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
# Load balancing is handled by Nginx (see docker-compose.load-balancer.yml
# and infrastructure/nginx/). No NestJS LOAD_BALANCER_* env vars are required.

OAUTH_TOKEN_ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHARS
